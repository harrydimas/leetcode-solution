name: Java CI with Maven

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    environment: development
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Grant execute permission for Maven wrapper
        run: chmod +x mvnw

      - name: Validate Maven wrapper
        run: ./mvnw --version

      - name: Build with Maven
        run: ./mvnw clean package

#      - name: Analyze with SonarQube
#        run: |
#          ./mvnw sonar:sonar \
#            -Dsonar.host.url=http://localhost:9000 \
#            -Dsonar.token=sqa_bbce6450572f6f7d6342d07542f42c07088e45f9

      - name: print sonarqube report
        run: |
          echo "github.workspace=${{ github.workspace }}"
          echo "github.action_path=${{ github.action_path }}"
          echo "github.action=${{ github.action }}"
          echo "github.action_repository=${{ github.action_repository }}"

#      - name: SonarQube post action
#        uses: harrydimas/sonarqube-post-action@main
#        with:
#          file_path: '${{ github.workspace }}/target/sonar/report-task.txt'
#          sonar_token: 'sqa_bbce6450572f6f7d6342d07542f42c07088e45f9'
#          slack_webhook: 'https://hooks.slack.com/services/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'
#          slack_mention: '<!subteam^S050C8Z89MH>'
#          slack_channel: '#notif-sonarqube-dev'
#          pull_request_url: 'https://github.com/alami-group/mina-alami/pull/2762'
#          pull_request_title: 'feat: sonarqube integration'